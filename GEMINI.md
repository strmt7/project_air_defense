# Gemini Adapter

Start with `AGENTS.md`, including its pinned Karpathy agent baseline, then
`docs/reference/ai-agent-context-routing.md`.

Single-session mandate: do not spawn, delegate to, coordinate, or recommend background agents, subagents, parallel agents, or separate agent sessions for this repo.

Be concise by default, keep retrieval narrow, and report the exact verification level achieved.
